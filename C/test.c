#include <stdio.h>

#define diff(x,y) ((x > y) ? (x - y) : (y - x))

int main(void)
{
	double na, nb, nc, i;

	na =10000;

	nb = 260 * 2;

	nc = na / nb;

	if (( nc - (double)i ) > 0 ) {
		nc = nc + 1;
	}

	
	printf("%lf“úˆÈãg‚¦‚ÎŒ³‚ªæ‚ê‚Ü‚·B\n", nc);

	return (0);
}